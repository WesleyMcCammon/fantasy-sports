import { Route, withRouter } from 'react-router-dom';
export const CreateLineUpRoute = '/create-lineup';

function Routing() {
    return (
        <div>
            Yo
        </div>
    )
}

export default withRouter(Routing);

